@page
@model ITP.WebApp.Pages.Customers.IndexModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "Customers";
}

<h1>Customers</h1>

<a asp-page="Add">Add New Customber</a>
<!--
<form method="POST">
    <table class="table">
        <thead class="thead-dark">
            <tr>
                <th>DocType</th>
                <th>DocNumer</th>
                <th>Name</th>
                <th>Last Name</th>
                <th>Born Date</th>
                <th>City</th>
                <th>Ver</th>
                <th>Editar</th>
                <th>Eliminar</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Customers)
            {
            <tr>
                <td>@item.DocType</td>
                <td>@item.DocNumber</td>
                <td>@item.Name</td>
                <td>@item.LastName</td>
                <td>@item.BornDate</td>
                <td>@item.City</td>
                <td>
                    <a asp-page="../Insurances/Add"
                       asp-route-customerid="@item.Id"
                       class="waves-effect waves-light btn">
                        <i class="large material-icons">PageView</i>
                    </a>

                </td>
                <td>
                    <a asp-page="Edit"
                       asp-route-id="@item.Id"
                       class="waves-effect waves-light btn">
                        <i class="large material-icons">Edit</i>
                    </a>
                </td>
                <td>
                    <button type="submit"
                            class="btn btn-danger"
                            asp-page-handler="delete"
                            asp-route-id="@item.Id">
                        <i class="large material-icons">Delete</i>
                    </button>
                </td>
            </tr>
            }
        </tbody>
    </table>
</form>

    -->
